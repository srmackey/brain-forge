# Starting ignore for a new Brain Forge vault graph.
# Living layer: wiki/, raw/ (except _log.md), primers/.
# gitignore semantics (last-match-wins, ! to un-ignore).
#
# Install seeds this file at the vault root. Update never overwrites it,
# so anything you add here is safe.

# Installed framework (not vault knowledge)
_brain-forge/

# Seeded framework file living beside your own primers
primers/distill.md

# Host / runtime
.obsidian/
.claude/
.grok/
.cursor/
.vscode/
.git/

# Chair overlay, if present
_status/
inbox/
PROTOCOL.md
_system/

# Constitution is read directly. Indexing it creates a dominant hub.
AGENTS.md
CLAUDE.md

# Graphify output
_graphify-out/
graphify-out/
__graphify-out/

# Binary attachments
_attachments/

# Retained on disk, low signal for the living wiki layer
archive/
journal/

# Ingest reads this as text
raw/_log.md
